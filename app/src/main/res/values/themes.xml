<resources>

    <!-- main theme -->
    <style name="Theme.BarAudio" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- splash screen -->
    <style name="Theme.BarAudio.Splash" parent="Theme.SplashScreen" >
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.BarAudio</item>
    </style>
</resources>